{% extends 'base.html.twig' %}

{% block title %}PaiementCategory{% endblock %}

{% block body %}
    <h1>PaiementCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ paiement_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paiement_category_index') }}">back to list</a>

    <a href="{{ path('paiement_category_edit', {'id': paiement_category.id}) }}">edit</a>

    {{ include('paiement_category/_delete_form.html.twig') }}
{% endblock %}
