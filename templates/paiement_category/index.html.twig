{% extends 'base.html.twig' %}

{% block title %}PaiementCategory index{% endblock %}

{% block body %}
    <h1>PaiementCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for paiement_category in paiement_categories %}
            <tr>
                <td>{{ paiement_category.id }}</td>
                <td>{{ paiement_category.name }}</td>
                <td>
                    <a href="{{ path('paiement_category_show', {'id': paiement_category.id}) }}">show</a>
                    <a href="{{ path('paiement_category_edit', {'id': paiement_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('paiement_category_new') }}">Create new</a>
{% endblock %}
