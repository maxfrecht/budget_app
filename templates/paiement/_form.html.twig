{{ form_start(form) }}
<div class="form-group">
    {{ form_widget(form.amount, {'attr': {'placeholder': 'placeholder'}}) }}
    {{ form_label(form.amount, 'Montant') }}
</div>
<div class="form-group">
    {{ form_widget(form.user, {'attr': {'placeholder': 'placeholder'}}) }}
    {{ form_label(form.user, 'Titre') }}
</div>
<div class="form-group">
    {{ form_widget(form.category, {'attr': {'placeholder': 'placeholder'}}) }}
    {{ form_label(form.category, 'Titre') }}
</div>
    <button class="btn">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}
